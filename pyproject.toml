[project]
name = "uv-transitive-osv-repro"
version = "0.1.0"
requires-python = ">=3.11"
# A single, non-vulnerable direct dependency. It pulls in `idna` transitively.
dependencies = ["requests>=2.32.5"]
